Détail de la tâche :

<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<p> <%= link_to "back to the liste", tasks_path%> </p>

<p> <%= link_to "update", edit_path%> </p>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
